export const styles = {
  button: {
    color: (theme) => theme.palette.primary.main,
    "&:hover": {
      color: "white",
      backgroundColor: (theme) => theme.palette.primary.main,
    },
  },
};
